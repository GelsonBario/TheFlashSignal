import { StyleSheet } from 'react-native';

/**
 * Folha de estilos para o componente FlashLogo.
 */
export const styles = StyleSheet.create({
  container: {
    marginBottom: 50, // Espaçamento abaixo da logo.
    alignItems: 'center',
  },
  logo: {
    width: 150,
    height: 150,
  },
});